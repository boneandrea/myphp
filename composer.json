{
  "require": {
    "vlucas/phpdotenv": "*",
    "phpunit/phpunit": "*"
  },
  "scripts":{
    "myinit": "touch .env && composer config -g repos.packagist composer https://packagist.jp && composer global require hirak/prestissimo",
    "test": "phpunit tests/** --colors=always"
  }
}
